mixin info_block(block_class, header, value)
  label(class=block_class)
    .value.g-standardField!=value
    .m-title.g-edit-smallTitle.select=header

.block
  .location
    .m-title.g-edit-smallTitle.select Место :
    .value
      .tutor!= tutor
      .student!= student
      .web!= web

  +info_block('country', 'Страна :', country)
  +info_block('city', 'Город :', city)
  +info_block('district', 'Район :', district)
  +info_block('nearest_metro', 'Ближайшее метро :',  nearest_metro)
  +info_block('street', 'Улица :', street)

  .line
    +info_block('house', 'Дом :', house)
    +info_block('building', 'Строение :', building)

  //-.house_block
    label(for="house_label").house!= house

    .value
      label(for="house_label").house!= house
      label(for="building_label").building
        .value!= building
        .m-title.g-edit-smallTitle Строение :
    .m-title.g-edit-smallTitle Дом :
  .flat_block
    +info_block('flat', 'Кв. :', flat)

  .button_block
    .add_button!= add_button

  .button_block
    .save_button!= save_button



