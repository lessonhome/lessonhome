

h2 Ваш баланс:
.summ_price(class=parseInt(current_sum) < 0? 'red' : '')
  span.js-summ!=current_sum
  span &nbsp;руб.
.form
  .summ_send!=send_input
  span.curr &nbsp;руб.
  .btn_send!=send_btn

h2 История:
ul

h2 Детализация:
mixin trans(number, date, type, status, value, residue)
  tr
    td!=number
    td
      .time(style ="display: none")!=date
      p.local_date(style ="visibility: hidden") 00.00.0000
      i.local_time(style ="visibility: hidden") 00:00:00
    case type
      when 'fill'
        td.up Пополнение
      when 'pay'
        td.down Списание
      default
        td Прочие операции
    td #{value || '0.00'} руб.
    td!=residue? residue + " руб.": "&#8212;"

table.js-transations
  thead
    tr
      th Счет
      th Дата / Время
      th Операция
      th Сумма платежа
      th Остаток на счете
  tbody
    -var exist = false
    each t in transactions
      if t.status == 'success'
        -exist = true
        +trans(t.number, t.date, t.type, t.status, t.value, t.residue)
    if !exist
      tr
        td.empty(colspan='5') Операций не обнаружено