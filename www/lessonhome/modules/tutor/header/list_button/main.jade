
mixin R(t)
  - t = t.replace(/\s/g, '&nbsp;')
  .title!= t
.visible
  a(href= href)
    if $pageTag(tag)
      .title.active= title
    else
      .title= title
  .hidden
    .box_menu
      .triangle_box
        .triangle
      .menu
        each value in list
          !=value





//-
        each h,t in list
          a(href= h)
            +R(t)

