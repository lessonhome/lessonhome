// takes variables:
  //{selector, name, details}
// type variables
  //selector, name, details - string
//respons
  // type - style sass
  //name, details - content
.box_line.select(class=selector)
  .title_box
    if selector == 'address_time'
      .title!=title
    else
      .js-icon_box.unselect
        .icon
      .title!=title+' : '
  if details
    .details=details
  if title_two
    .title_two_box
      //-.icon_box.unselect
        .icon
      .title_two!=title_two