- selector = selector || "";
- placeholder = placeholder || "";
label(class=selector)
  if text
    .text!=text
  if height
    textarea(style="height : #{height}", placeholder=placeholder)
  else
    textarea(style="height : 100%", placeholder=placeholder)
