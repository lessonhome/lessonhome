// takes variables:
  //{section, selector}
// type variables
  // section - array
  //selector - string
//respons
  // section - content
  // selector - style sass
each val, key in section
  .section(class=selector)
    .title!=key
    .value!=val