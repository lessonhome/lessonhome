// takes variables:
  //{selector, text}
// type variables
  // selector, text  - string
//respons
  //text - content
  // selector - style sass
//style="background: url(#{F('main/pattern_dots_profilebutton.png')})"
.button(class=selector)
  if close
    .close
      .close_background
  if text
    .text!=text