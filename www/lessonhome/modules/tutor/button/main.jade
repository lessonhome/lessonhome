// takes variables:
  //{selector, text}
// type variables
  // selector, text  - string
//respons
  //text - content
  // selector - style sass
//style="background: url(#{F('main/pattern_dots_profilebutton.png')})"
- selector = selector || '';
.button.unselect(class=selector)
  if close
    .close
      .close_background
  if text
    .text!=text