// takes variables:
  //{selector, text}
// type variables
  // selector, text  - string
//respons
  //text - content
  // selector - style sass
.button(class=selector)
  if close
    .close
      .close_background
  .text!=text
