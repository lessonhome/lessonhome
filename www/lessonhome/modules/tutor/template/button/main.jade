// takes variables:
  //{selector, text}
// type variables
  // selector, text  - string
//respons
  //text - content
  // selector - style sass


if selector == 'add_subject'
  .button(class=selector).plus +
    .text=text
else
  .button(class=selector)
    .text=text