mixin field (selector, value)
  .value(class=selector)!= value
/////////////
if address_popup
  .background
    .address_box
      .title_box
        .title Укажите адрес
        a(href = '/tutor/profile/second_step')
          .close_bg

      .address_popup!=address_popup
+field ('country', country)
+field ('city', city)
.title_box
  .title Адрес :
  a(href = '/tutor/profile/second_step_address') Укажите подробный адресс
.line_box
  .line!= line
+field ('mobile_phone', mobile_phone)
+field ('extra_phone', extra_phone)
+field ('post', post)
+field ('skype',skype)
+field ('site',site)
