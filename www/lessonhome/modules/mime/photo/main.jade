- var max = 55,a;
- if (photo){
- var w = photo.width,h = photo.height;
- }else {
- var w = max,h = max;
- }
- if (w >= h){ if(w>max){a=max/w; w*=a;h*=a;} }
- else if (h > w){ if(h>max){a=max/h; w*=a;h*=a;} }
if photo
  img(src=photo.url style="width:#{w}px;height:#{h}px")
else
  img(src=src style="width:auto;height:#{max}px")


