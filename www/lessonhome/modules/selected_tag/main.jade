// takes variables:
  //{selector, text}
// type variables
  // selector, text  - string
//respons
  //text - content
  // selector - style sass
- id = id || "";

.selected_tag(id=id, class=selector)
  if close
    .close
      .close_background
  .text=text